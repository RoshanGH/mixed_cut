# 撤销 / 重做 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 MixCut 加商业级撤销/重做（⌘Z / ⌘⇧Z、多级、编辑菜单、Toast 撤销按钮），覆盖所有数据改动；删除改为延后清理磁盘文件（孤儿 GC），保证删除可完整恢复。

**Architecture:** 给 SwiftData `mainContext` 挂一个全局 `NSUndoManager`，SwiftData 自动登记增删改；自定义编辑菜单驱动 undo/redo 并在之后发通知让当前视图重载。删除路径只删记录、不再 inline 删文件；启动时用孤儿 GC 回收无引用文件。孤儿判断抽成 MixCutCore 纯函数单测。

**Tech Stack:** Swift 5.10 / SwiftUI / SwiftData / NSUndoManager / Swift Testing。

设计依据：`docs/superpowers/specs/2026-06-04-undo-redo-design.md`

> 说明（对 spec 的一处简化）：GC 只在**启动时**跑（启动即可回收上一会话产生的孤儿，足够），不做退出时 GC，省去 App delegate 复杂度。

---

## 文件结构

**新增/改（纯逻辑，swift test）：**
- `Sources/MixCutCore/OrphanFileFinder.swift` — `orphanFiles(onDisk:referenced:)` 纯函数
- `Tests/MixCutCoreTests/OrphanFileFinderTests.swift` — 单测
- App target 已直接编译 `Sources/MixCutCore/*.swift`（同模块，无需 import）

**改（App，xcodebuild + 真机验证）：**
- `MixCut/App/MixCutApp.swift` — 建 `appUndoManager` 挂 mainContext；`.commands` 加撤销/重做菜单；启动序列尾部跑 GC
- `MixCut/Utilities/FileHelper.swift` — 新增 `collectOrphanFiles(referencedPaths:)`（扫描视频/缩略图目录 → 调 `orphanFiles` → 删孤儿）
- `MixCut/ViewModels/ProjectViewModel.swift`、`ImportViewModel.swift`、`SegmentLibraryViewModel.swift` — 删除路径移除 inline 删文件、加 `setActionName` + Toast 撤销
- `MixCut/ViewModels/SchemeViewModel.swift` — 各编辑/生成操作 `setActionName`、生成用 undo grouping
- `MixCut/Views/Shared/Toast.swift` — `ToastPayload` + `show` 增可选撤销按钮，`ToastOverlay` 渲染按钮
- `MixCut/App/Notifications`（现有通知定义处）— 加 `.mixCutDataDidUndo`

---

## Task 1: MixCutCore 孤儿文件纯函数

**Files:**
- Create: `Sources/MixCutCore/OrphanFileFinder.swift`
- Test: `Tests/MixCutCoreTests/OrphanFileFinderTests.swift`

- [ ] **Step 1: 写失败测试**

`Tests/MixCutCoreTests/OrphanFileFinderTests.swift`:
```swift
import Testing
@testable import MixCutCore

@Test func orphan_returnsFilesNotReferenced() {
    let onDisk = ["/v/a.mp4", "/v/b.mp4", "/t/x.jpg"]
    let referenced: Set<String> = ["/v/a.mp4", "/t/x.jpg"]
    #expect(OrphanFileFinder.orphanFiles(onDisk: onDisk, referenced: referenced) == ["/v/b.mp4"])
}

@Test func orphan_emptyWhenAllReferenced() {
    let onDisk = ["/v/a.mp4"]
    #expect(OrphanFileFinder.orphanFiles(onDisk: onDisk, referenced: ["/v/a.mp4"]).isEmpty)
}

@Test func orphan_normalizesTrailingSlashAndCase() {
    // 末尾斜杠 + 大小写差异应视为同一路径（macOS 默认大小写不敏感）
    let onDisk = ["/V/A.mp4/"]
    let referenced: Set<String> = ["/v/a.mp4"]
    #expect(OrphanFileFinder.orphanFiles(onDisk: onDisk, referenced: referenced).isEmpty)
}

@Test func orphan_allUnreferencedReturnedInInputOrder() {
    let onDisk = ["/v/b.mp4", "/v/a.mp4"]
    #expect(OrphanFileFinder.orphanFiles(onDisk: onDisk, referenced: []) == ["/v/b.mp4", "/v/a.mp4"])
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `swift test --filter orphan 2>&1 | tail -8`
Expected: 找不到 `OrphanFileFinder`，编译失败。

- [ ] **Step 3: 写实现**

`Sources/MixCutCore/OrphanFileFinder.swift`:
```swift
import Foundation

/// 找出"磁盘上存在但数据库已无任何记录引用"的孤儿文件（供延后删除 GC 使用）。
/// 安全原则：判断不确定时保守不删——只返回明确未被引用的。
public enum OrphanFileFinder {
    /// 归一化路径：去末尾斜杠 + 小写（macOS APFS 默认大小写不敏感）
    private static func normalize(_ path: String) -> String {
        var p = path
        while p.count > 1 && p.hasSuffix("/") { p.removeLast() }
        return p.lowercased()
    }

    /// - Parameters:
    ///   - onDisk: 磁盘上扫描到的文件绝对路径
    ///   - referenced: 数据库记录引用到的路径集合
    /// - Returns: onDisk 中未被引用的路径（保持 onDisk 原始顺序与原始字符串）
    public static func orphanFiles(onDisk: [String], referenced: Set<String>) -> [String] {
        let refNorm = Set(referenced.map(normalize))
        return onDisk.filter { !refNorm.contains(normalize($0)) }
    }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `swift test --filter orphan 2>&1 | tail -5`
Expected: 4 个测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add Sources/MixCutCore/OrphanFileFinder.swift Tests/MixCutCoreTests/OrphanFileFinderTests.swift
git commit -m "feat(core): orphanFiles 孤儿文件判断（延后删除 GC 用）"
```

---

## Task 2: FileHelper 孤儿 GC（扫描 + 删除）

**Files:**
- Modify: `MixCut/Utilities/FileHelper.swift`

- [ ] **Step 1: 加 GC 方法**

在 `FileHelper` 内新增（复用现有 `globalVideoDirectory` / `globalThumbnailDirectory`）：
```swift
/// 回收孤儿文件：扫描全局视频/缩略图目录，删除"数据库已无任何记录引用"的文件。
/// referencedPaths 由调用方从所有 Video/Segment 记录收集（localPath/thumbnailPath/segment.thumbnailPath）。
static func collectOrphanFiles(referencedPaths: Set<String>) {
    let fm = FileManager.default
    var onDisk: [String] = []
    // 视频：Videos/{hash}/xxx.mp4（两级）
    if let hashDirs = try? fm.contentsOfDirectory(at: globalVideoDirectory, includingPropertiesForKeys: nil) {
        for hashDir in hashDirs {
            if let files = try? fm.contentsOfDirectory(at: hashDir, includingPropertiesForKeys: nil) {
                onDisk.append(contentsOf: files.map(\.path))
            }
        }
    }
    // 缩略图：Thumbnails/xxx
    if let thumbs = try? fm.contentsOfDirectory(at: globalThumbnailDirectory, includingPropertiesForKeys: nil) {
        onDisk.append(contentsOf: thumbs.map(\.path))
    }

    let orphans = OrphanFileFinder.orphanFiles(onDisk: onDisk, referenced: referencedPaths)
    for path in orphans {
        try? fm.removeItem(atPath: path)
    }
    // 清理空的 hash 子目录
    if let hashDirs = try? fm.contentsOfDirectory(at: globalVideoDirectory, includingPropertiesForKeys: nil) {
        for hashDir in hashDirs where (try? fm.contentsOfDirectory(atPath: hashDir.path))?.isEmpty == true {
            try? fm.removeItem(at: hashDir)
        }
    }
    if !orphans.isEmpty { MixLog.info("GC 回收孤儿文件 \(orphans.count) 个") }
}
```

- [ ] **Step 2: 编译验证**

Run: `xcodebuild -project MixCut.xcodeproj -scheme MixCut -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 3: Commit**

```bash
git add MixCut/Utilities/FileHelper.swift
git commit -m "feat: FileHelper 孤儿文件 GC（扫描+删除无引用文件）"
```

---

## Task 3: 删除路径改为只删记录（移除 inline 删文件）

**Files:**
- Modify: `MixCut/ViewModels/ProjectViewModel.swift`（~107-110）
- Modify: `MixCut/ViewModels/ImportViewModel.swift`（~597-603）
- Modify: `MixCut/ViewModels/SegmentLibraryViewModel.swift`（deleteSegment ~356 / deleteSelectedSegments ~370）

- [ ] **Step 1: ProjectViewModel.deleteProject 移除删文件**

删掉对 `FileHelper.deleteGlobalVideoFiles(...)` 与 `try? FileManager.default.removeItem(atPath: path)`（分镜缩略图）的调用，**只保留 `context.delete(...)` 删记录**。其余逻辑（收集 referencedVideos、级联删 Segment/Video 记录、`deleteProjectDirectory` 旧目录清理保留）不变。

- [ ] **Step 2: ImportViewModel.deleteVideo 移除删文件**

删掉 `FileHelper.deleteGlobalVideoFiles(...)`（598）与缩略图 `removeItem(atPath: thumbPath)`（602），只删记录。

- [ ] **Step 3: SegmentLibraryViewModel 删分镜移除删缩略图**

`deleteSegment` / `deleteSelectedSegments` 里若有删除 `segment.thumbnailPath` 文件的代码，移除，只 `context.delete(segment)`。

- [ ] **Step 4: 编译验证**

Run: `xcodebuild -project MixCut.xcodeproj -scheme MixCut -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 5: Commit**

```bash
git add MixCut/ViewModels/ProjectViewModel.swift MixCut/ViewModels/ImportViewModel.swift MixCut/ViewModels/SegmentLibraryViewModel.swift
git commit -m "refactor: 删除只删记录，磁盘文件交给孤儿 GC（为撤销可恢复铺路）"
```

---

## Task 4: 挂 UndoManager + 编辑菜单 + 撤销后重载通知

**Files:**
- Modify: `MixCut/App/MixCutApp.swift`
- Modify: 通知定义处（搜 `mixCutNavigate` 所在文件，加 `mixCutDataDidUndo`）

- [ ] **Step 1: 加通知名**

在定义 `Notification.Name.mixCutNavigate` 的扩展里追加：
```swift
static let mixCutDataDidUndo = Notification.Name("mixCutDataDidUndo")
```

- [ ] **Step 2: MixCutApp 建 appUndoManager 挂 mainContext**

在 `MixCutApp` 加存储属性并在 init 成功分支赋值：
```swift
let appUndoManager = UndoManager()
```
init 里 `modelContainer` 创建成功后：
```swift
appUndoManager.levelsOfUndo = 0   // 不限层数
modelContainer.mainContext.undoManager = appUndoManager
```
（注意 init 里 `appUndoManager` 是 let，需在声明处初始化或 init 内赋值；用声明处 `let appUndoManager = UndoManager()` 最简。）

- [ ] **Step 3: 加编辑菜单撤销/重做命令**

在现有 `.commands { ... }` 内追加：
```swift
CommandGroup(replacing: .undoRedo) {
    Button(undoTitle) { performUndo() }
        .keyboardShortcut("z", modifiers: .command)
        .disabled(!appUndoManager.canUndo)
    Button(redoTitle) { performRedo() }
        .keyboardShortcut("z", modifiers: [.command, .shift])
        .disabled(!appUndoManager.canRedo)
}
```
并加辅助计算属性/方法（在 MixCutApp 内）：
```swift
private var undoTitle: String { "撤销" + (appUndoManager.undoActionName.isEmpty ? "" : " " + appUndoManager.undoActionName) }
private var redoTitle: String { "重做" + (appUndoManager.redoActionName.isEmpty ? "" : " " + appUndoManager.redoActionName) }
private func performUndo() {
    guard appUndoManager.canUndo else { return }
    appUndoManager.undo()
    try? modelContainer.mainContext.save()
    NotificationCenter.default.post(name: .mixCutDataDidUndo, object: nil)
}
private func performRedo() {
    guard appUndoManager.canRedo else { return }
    appUndoManager.redo()
    try? modelContainer.mainContext.save()
    NotificationCenter.default.post(name: .mixCutDataDidUndo, object: nil)
}
```

- [ ] **Step 4: 当前视图监听重载**

在主内容视图（`ContentView` 或当前承载各功能视图的容器）加：收到 `.mixCutDataDidUndo` 时，对当前选中 project 重新触发各 VM 的 load（`projectVM.fetchProjects()` + 当前页对应 VM 的 `loadXXX(for: project)`）。复用现有"切换项目"重载路径。

- [ ] **Step 5: 编译验证**

Run: `xcodebuild -project MixCut.xcodeproj -scheme MixCut -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 6: Commit**

```bash
git add MixCut/App/MixCutApp.swift <通知文件>
git commit -m "feat: 挂全局 UndoManager + 编辑菜单 ⌘Z/⌘⇧Z + 撤销后重载"
```

---

## Task 5: 为各操作命名 + 生成成组

**Files:**
- Modify: `MixCut/ViewModels/*.swift`（各可撤操作处）

- [ ] **Step 1: 删除/编辑/重命名加 setActionName**

在每个数据改动**之前**调 `modelContext?.undoManager?.setActionName("…")`，例如：
- `deleteProject` → `"删除项目"`；`deleteVideo` → `"删除视频"`；`deleteSegment` → `"删除分镜"`；`deleteSelectedSegments` → `"删除分镜"`；`deleteScheme` → `"删除方案"`；`deleteStrategy` → `"删除策略"`；`removeSegment` → `"移除分镜"`
- `setStartTime`/`setEndTime` → `"修改时间"`；`toggleSemanticType` → `"修改类型"`；`updatePositionType` → `"修改位置"`；`renameProject` → `"重命名项目"`；`moveSegment` → `"调整顺序"`；`insertSegment` → `"插入分镜"`

- [ ] **Step 2: AI 生成用 undo grouping**

`generateSchemes` / `generateNarrativeVariants` 落库前后用：
```swift
let um = modelContext?.undoManager
um?.beginUndoGrouping()
um?.setActionName("生成方案")
// ... 批量 insert MixScheme/SchemeSegment ...
um?.endUndoGrouping()
```
确保整批方案一次 ⌘Z 撤销。

- [ ] **Step 3: 编译验证**

Run: `xcodebuild -project MixCut.xcodeproj -scheme MixCut -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`。

- [ ] **Step 4: Commit**

```bash
git add MixCut/ViewModels
git commit -m "feat: 为可撤操作命名 + AI 生成成组撤销"
```

---

## Task 6: Toast 撤销按钮

**Files:**
- Modify: `MixCut/Views/Shared/Toast.swift`
- Modify: 删除调用处（弹"已删除 · 撤销"）

- [ ] **Step 1: ToastPayload + show 增可选撤销动作**

`ToastPayload` 加 `let actionTitle: String?` 与 `let action: (@MainActor () -> Void)?`（Equatable 比较忽略闭包，按 id 比较即可——`static func ==` 用 `lhs.id == rhs.id`）。`show` 增参数：
```swift
func show(_ text: String, icon: String = "checkmark.circle.fill",
          style: InlineBanner.Style = .success, duration: Double = 2.2,
          actionTitle: String? = nil, action: (@MainActor () -> Void)? = nil) { ... 把 actionTitle/action 存进 payload ... }
```
`ToastOverlay` 在文案后：若 `payload.actionTitle != nil`，渲染一个文字按钮，点了执行 `payload.action?()` 并清除当前 toast。

- [ ] **Step 2: 删除处弹撤销 Toast**

在 deleteSegment/deleteProject/deleteVideo/deleteScheme 等成功后：
```swift
ToastCenter.shared.show("已删除", icon: "trash.fill", style: .warning,
    actionTitle: "撤销", action: { [weak self] in self?.modelContext?.undoManager?.undo(); try? self?.modelContext?.save() })
```
（撤销后同样发 `.mixCutDataDidUndo` 让视图重载——可在 action 里 post。）

- [ ] **Step 3: 编译验证 + 真机重启**

Run: `xcodebuild ... build`（`** BUILD SUCCEEDED **`），然后 `pkill -x MixCut; open <Debug app>`。

- [ ] **Step 4: Commit**

```bash
git add MixCut/Views/Shared/Toast.swift MixCut/ViewModels
git commit -m "feat: 删除后 Toast 撤销按钮"
```

---

## Task 7: 启动时跑孤儿 GC

**Files:**
- Modify: `MixCut/App/MixCutApp.swift`（启动迁移序列尾部，如 `recoverFromDisk` 之后）

- [ ] **Step 1: 加 GC 调用**

新增静态方法并在启动序列尾部调用：
```swift
private static func runOrphanGC(container: ModelContainer) {
    let ctx = container.mainContext
    var referenced = Set<String>()
    if let videos = try? ctx.fetch(FetchDescriptor<Video>()) {
        for v in videos {
            referenced.insert(v.localPath)
            if let t = v.thumbnailPath { referenced.insert(t) }
        }
    }
    if let segs = try? ctx.fetch(FetchDescriptor<Segment>()) {
        for s in segs { if let t = s.thumbnailPath { referenced.insert(t) } }
    }
    FileHelper.collectOrphanFiles(referencedPaths: referenced)
}
```
在 init 成功分支、其它 migration 之后调用 `Self.runOrphanGC(container: modelContainer)`。

- [ ] **Step 2: 编译 + 真机重启**

Run: `xcodebuild ... build`（SUCCEEDED），`pkill -x MixCut; open <Debug app>`。

- [ ] **Step 3: Commit**

```bash
git add MixCut/App/MixCutApp.swift
git commit -m "feat: 启动时孤儿文件 GC（回收已删无引用文件）"
```

---

## Task 8: 端到端人工验收

**Files:** 无（验证）

- [ ] **Step 1: swift test 回归**

Run: `swift test 2>&1 | tail -3`
Expected: 全 PASS（含 orphan 与之前所有）。

- [ ] **Step 2: 真机走查（编译后我自动重启再交付）**

1. 删分镜 → ⌘Z 恢复；连删多个 → 连续 ⌘Z 全恢复；⌘⇧Z 重做。
2. 删项目/视频 → ⌘Z 恢复，**恢复后视频能播放**（延后删除生效）。
3. 改时间/类型、重命名 → ⌘Z 撤回。
4. AI 生成方案 → ⌘Z 一次撤掉整批。
5. 编辑菜单"撤销 X / 重做 X"动态标题与启用态正确。
6. 删除后 Toast"撤销"按钮生效。
7. 重启后 GC 回收了已删无引用文件、未误删在用文件（对比磁盘）。
8. 切项目后撤销不串数据、当前视图正确刷新。

- [ ] **Step 3: 最终编译**

Run: `xcodebuild -project MixCut.xcodeproj -scheme MixCut -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`。

---

## 自检（spec 覆盖）

- ⌘Z/⌘⇧Z + 编辑菜单 + 动态标题 → Task 4 ✓
- 多级栈/全局单栈 → Task 4（levelsOfUndo=0，挂 mainContext）✓
- 覆盖所有数据改动 + 命名 + 生成成组 → Task 5 ✓
- 延后删除 + 孤儿 GC（纯函数单测 + 启动回收）→ Task 1/2/3/7 ✓
- Toast 撤销按钮 → Task 6 ✓
- 撤销后视图重载 → Task 4 Step 4 + Task 6 ✓
- GC 保守不误删 → Task 1（归一化匹配）+ Task 2（只删明确无引用）✓
- 范围边界（导入/导出/UI 瞬态不纳入）→ 计划未触及这些路径，符合 ✓
